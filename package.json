{
  "name": "pure-function-visualizer",
  "version": "0.1.0",
  "description": "Visually connect pure function modules via inputs and outputs",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "browser-sync start --server --files '**/*.css,**/*.js,**/*.ts,**/*.html' --no-ghost-mode --no-notify"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "browser-sync": "^2.11.1"
  }
}
