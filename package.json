{
  "name": "input-output",
  "version": "0.1.0",
  "description": "Visually connect pure functions modules via inputs and outputs",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "browser-sync start --server --index index.html --files '**/*.css,**/*.js,**/*.ts,**/*.html' --no-ghost-mode --no-notify"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.11.1"
}
}
